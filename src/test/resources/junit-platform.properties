# Enable parallel execution
junit.jupiter.execution.parallel.enabled=true

# Run test classes in parallel by default
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Strategy for parallelism - dynamic adapts to available processors
junit.jupiter.execution.parallel.config.strategy=dynamic

# Use 75% of available processors
junit.jupiter.execution.parallel.config.dynamic.factor=0.75

# Maximum pool size for parallel execution
junit.jupiter.execution.parallel.config.fixed.max-pool-size=256

# Timeout for tests (prevent hanging)
junit.jupiter.execution.timeout.default=5m
junit.jupiter.execution.timeout.mode=enabled